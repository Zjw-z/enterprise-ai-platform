"""Optional custom Agent implementation for multi-tool-trip-agent.

The declarative agent.yaml is used by default. Add a custom BaseAgent implementation here only when orchestration code is required.
"""
